
fn main() -> int {
    let x: int;
    let y = function(11);
    x = function(-3);
    let z = function(0);

    return x + y + z;
}

fn function(a: int) -> int {
    if (a > 10) {
        return 5;
    } elif (a < -2) {
        return 1;
    } else {
        return -3;
    }
}